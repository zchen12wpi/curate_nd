require 'spec_helper_features'

describe 'user profile behavior', FeatureSupport.options do
  context 'new user' do
    it 'behaves as expected' do
      # go to homepage
      # click login
      # accept terms of service
      # update my profile
      # upload a new dataset
      # then I add a section to my profile
      # and I add the dataset to my profile
      # and for extra credit my profile does not show up in search results
    end
  end
end
