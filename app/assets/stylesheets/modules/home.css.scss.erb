@import 'global-variables';

.hero {
  background-color: $official-blue;
  background-image: none;
  background-position: top center;
  background-repeat: no-repeat;
  background-size: 100%;
  clear: both;
  float: left;
  margin: 0;
  padding: 1em 0 0;
  position: relative;
  width: 100%;
}

@media (min-width: 700px) {
  .hero {
    background-image: url(<%= asset_path 'hero/basilica-dome-quad-1000.jpg'%>);
    padding-top: 2em;
    background-size:cover;
  }
}

@media (min-width: 1200px) {
  .hero {
    background-image: url(<%= asset_path 'hero/basilica-dome-quad-1500.jpg'%>);
    padding-top: 3em;
  }
}

$logo-width: 221;
$logo-height: 48;

.logo-wrapper {
  font-size: 1em;
  margin: 0 auto 1em;
  padding: 0;
  padding-top: percentage(($logo-height / $logo-width * 60) / 100);
  position: relative;
  width: 60%;

  .curate-nd-logo {
    height: 100%;
    left: 0;
    position: absolute;
    top: 0;
    width: 100%;
  }
}

@media (min-width: 1200px) {
  .logo-wrapper {
    padding-top: percentage(($logo_height / $logo_width * 40) / 100);
    width: 39%;
    margin-bottom: 3em;
  }
}

.im-looking-for {
  clear: both;
  float: left;
  font-size: 18px;
  margin-top: 3em;
  padding: 1em;
  width: 100%;
  box-sizing: border-box;

  h2 {
    text-align: center;
    color: $official-gold;
  }

  .categories {
    clear: both;
    float: left;
    list-style: none;
    margin: 0;
    padding: 0;
    text-align: center;
  }

  .category-list {
    clear: both;
    float: left;
    margin: 0;
    padding-top: 1em;
  }

  .category {
    clear: both;
    color: $official-gold;
    display: block;
    float: left;
    height: 5em;
    margin: 0 .6em;
    text-align: left;
  }

  .category-icon {
    float: left;
    width: 4em;
  }

  .caption {
    color: $official-gold;
    display: inline;
    float: left;
    font-size: 1em;
    line-height: 4;
    margin: 0;
    padding-left: .5em;
  }

  .catalog-browse {
    display: block;
    float: left;
    outline: 2px solid transparent;
    padding: 0 .5em;
    width: 100%;

    &:focus,
    &:hover {
      outline-color: $blue-accent;
    }

    &:focus,
    &:hover,
    &:visited {
      text-decoration: none;
    }
  }
}

@media (min-width: 500px) {
  .im-looking-for {
    padding: 2em;

    .category {
      display: inline-block;
      height: auto;
      padding-left: 0;
      text-align: center;
      width: 8em;
    }

    .category-icon {
      left: auto;
      position: relative;
      top: auto;
      width: 100%;
    }

    .caption {
      line-height: 1.2;
      display: block;
      padding-left: 0;
    }

    .catalog-browse {
      display: inline-block;
      padding: 0 .5em 2em;
    }

    .hero,
    .im-looking-for,
    .categories,
    .category-list,
    .category,
    .caption,
    .category-icon,
    .catalog-browse {
      float: none;
    }
  }
}

@media (min-width: 700px) {
  .im-looking-for {
    background-color: rgba(0, 31, 66, .8);
  }
}

@media (min-width: 900px) {
  .im-looking-for {
    h2 {
      font-size: 2.3em;
    }

    .category {
      width: 14em;
    }

    .caption {
      font-size: 1.8em;
    }
  }
}

@media (min-width: 1200px) {
  .im-looking-for {
    .category {
      margin: 0 1em;
    }
  }
}

.dark-blue {
  background-color: $blue-dark;
  clear: both;
  color: $white;

  h1 {
    color: $white;
    padding: 1em .5em 0;
    text-align: center;
  }

  hr {
    border-top: 2px solid $official-gold;
    border-bottom: 1px solid $official-gold-accent;
    margin: 1em auto;
    max-width: 33em;
  }

  h1 + hr {
    margin-top: 3em;
  }
}

@media (max-width: 900px) {
  .dark-blue {
    h1 {
      font-size: 22px;
      line-height: 1.4;
    }

    h1 + hr {
      margin-top: 2em;
      max-width: 16em;
    }
  }
}
