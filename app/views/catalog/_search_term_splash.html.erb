<% splash = Catalog::SearchSplashPresenter.call(params) %>
<%= debug params[:f][:human_readable_type_sim] %>
<%= debug params[:f_inclusive][:human_readable_type_sim] %>
<% if splash %>
  <div class="search-term-splash">
    <h1>
      <%= splash.html_safe %>
    </h1>
  </div>
  <%= render partial: 'shared/site_search', locals: { placeholder: "Search #{splash}" } %>
<% else %>
  <%= render 'shared/site_search' %>
<% end %>
