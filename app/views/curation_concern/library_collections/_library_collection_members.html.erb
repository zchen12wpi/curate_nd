<div class="library-collection-members">
  <h2>Library Collection Members</h2>
  <ul>
    <% curation_concern.library_collection_members.each do |member| %>
      <li><%= link_to(member, polymorphic_path([:curation_concern, member])) %></li>
    <% end %>
  </ul>
</div>
