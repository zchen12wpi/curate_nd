<% content_for :layout_name, 'dashboard, ' %>
<% content_for :main do %>

  <div class="row">

  <% if content_for?(:sidebar) %>
    <div class="col-sm-3">
      <%= yield(:sidebar) %>
    </div>

    <div class="col-sm-9">
  <% else %>
    <div class="col-sm-12">
  <% end %>
      <%= yield %>
    </div>

  </div>

<% end %>

<%= render template: "layouts/#{theme}" %>
